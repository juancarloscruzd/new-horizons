import React from 'react';

import styles from '../styles/layout.module.scss';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <small>New Horizons - 2023</small>
    </footer>
  );
};

export default Footer;
